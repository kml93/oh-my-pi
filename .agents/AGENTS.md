# Repository Overview

- **Project**: Dual-upstream fork based on `can1357/oh-my-pi` (`omp`) with selective feature ports from `earendil-works/pi` (`pi`).
- **Core CLI Binary**: `omp` (`packages/coding-agent/src/cli.ts`).
- **Primary Package**: `packages/coding-agent/`.
- **Git Remotes**:
  - `origin`: Personal fork (`git@github.com:kml93/oh-my-pi.git`).
  - `upstream-omp`: Official `can1357/oh-my-pi`.
  - `upstream-pi`: Official `earendil-works/pi`.
